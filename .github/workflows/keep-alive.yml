name: ProbLearn Keep Alive

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Ping ProbLearn
        run: |
          curl --max-time 60 \
            -w "\nHTTP STATUS: %{http_code}\n" \
            "https://problearn-ai.vercel.app/api/topics"
